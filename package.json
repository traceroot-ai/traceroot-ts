{
  "name": "traceroot-ts",
  "private": true,
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm --filter @traceroot/sdk build",
    "test": "pnpm --filter @traceroot/sdk test",
    "lint": "pnpm --filter @traceroot/sdk lint",
    "typecheck": "pnpm --filter @traceroot/sdk typecheck",
    "clean": "pnpm --filter @traceroot/sdk clean"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "pnpm": {
    "packageExtensions": {
      "@arizeai/openinference-instrumentation-openai": {
        "peerDependencies": {
          "openai": ">=6.0.0"
        }
      }
    }
  }
}
