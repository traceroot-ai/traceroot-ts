node_modules/
*.log
.DS_Store
.env
.env.local
dist/

# pnpm-only: the workspace root holds the single pnpm-lock.yaml. A package-level
# npm/yarn lockfile is dead weight pnpm ignores and silently drifts from package.json.
package-lock.json
npm-shrinkwrap.json
yarn.lock
