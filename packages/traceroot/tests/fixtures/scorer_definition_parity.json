{
  "_comment": "Shared cross-SDK normalized-definition parity. Equivalent Python and TypeScript scorer definitions must produce the SAME normalized descriptor fields below (identity + policy). Provenance (language/source) and the config-hash version are language-specific and NOT compared here.",
  "code": {
    "key": "grade",
    "scorer_type": "code",
    "value_type": "numeric",
    "direction": "higher_is_better",
    "threshold": 1.0
  },
  "static_judge": {
    "key": "nc",
    "scorer_type": "llm_judge",
    "model": "claude-sonnet-5",
    "output_type": "score",
    "threshold": 1.0,
    "direction": "higher_is_better"
  }
}
